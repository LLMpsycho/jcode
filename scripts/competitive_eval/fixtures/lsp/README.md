# Phase 2 LSP tasks

These deterministic fixtures exercise the supported semantic subset shared by
Jcode and OMP. They intentionally require the `lsp` capability and verify only
workspace outcomes. Run `run_campaign --dry-run` before any live campaign.

Live runs may invoke paid model providers. They must use explicit binaries and
should record the provider, model, effort, and service tier in the campaign.
