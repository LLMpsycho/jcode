Use language-server diagnostics to find and fix the type error in `src/app.py`.
The intended behavior is for `greet("Ada")` to return `"Ada!"`. Preserve the
typed public function and do not run the program to discover the error. Do not
modify the evaluation task, prompt, or verifier files.
