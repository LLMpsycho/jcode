Use the language server's semantic rename operation to rename the public Rust
function `calculate` to `compute` across the project. Read every affected file
first if your edit safety policy requires it. Do not use a plain text search and
replace. Run `cargo check` after the rename. Do not modify the evaluation task,
prompt, or verifier files.
